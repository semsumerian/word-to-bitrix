# Word -> Bitrix HTML

Локальный MVP-инструмент для автоматической подготовки HTML из Word-файлов справочника `Курсы обучения` для публикации в Bitrix Learning.

Инструмент нужен, чтобы сотрудники не переносили большие Word-документы в HTML вручную. Он читает Word-файл, применяет правила актуализации, формирует чистый HTML для поля `DETAIL_TEXT` и показывает отчет по изменениям.

## Для Кого

**Аналитик / инициатор актуализации**

- Загружает Word-файл.
- Проверяет предпросмотр.
- Смотрит, что красное удалено, желтое опубликовано.
- Передает HTML техподдержке или вставляет его в Bitrix при наличии доступа.

**Техподдержка / администратор Bitrix**

- Запускает локальный сайт.
- Проверяет HTML и отчет.
- Вставляет HTML в урок Bitrix Learning.
- При ошибках смотрит предупреждения и логику диагностики ниже.

## Что Делает Инструмент

- Принимает `.doc`, `.docx`, `.rtf`.
- Для старых `.doc` использует LibreOffice и переводит документ во внутренний `.docx`.
- Читает структуру Word напрямую из OOXML, а не копирует грязный HTML из Word.
- Удаляет фрагменты, выделенные красным или зачеркнутые.
- Оставляет фрагменты, выделенные желтым, но убирает саму подсветку.
- Удаляет красные строки таблиц до пересчета объединенных ячеек.
- Пересчитывает `rowspan`, чтобы таблицы не ехали после удаления строк.
- Сохраняет таблицы, ссылки, жирный, курсив, подчеркивание, базовые цвета и выравнивание.
- Сохраняет автонумерацию Word, например `1.` и `2.`, если номер хранится не текстом, а как список Word.
- Формирует структурированный HTML с отступами и переносами строк.
- Показывает предпросмотр HTML на сайте.
- Генерирует JSON-отчет по удаленным и добавленным фрагментам.

## Главные Правила Обработки

| В Word | Что будет в HTML |
| --- | --- |
| Красная заливка / красное выделение | Фрагмент удаляется |
| Зачеркнутый текст | Фрагмент удаляется |
| Желтая заливка / желтое выделение | Текст остается, подсветка удаляется |
| Обычный текст | Текст остается |
| Таблицы | Структура сохраняется |
| Красные строки таблицы | Строки удаляются, `rowspan` пересчитывается |
| Ссылки | Ссылки сохраняются |
| Автонумерация Word | Номера вставляются в HTML как обычный текст |

## Где Лежит Проект

```text
/Users/semen/Курсы обучения/word-to-bitrix
```

Основные файлы:

```text
bitrix_converter.py   ядро конвертера и CLI-запуск
server.py             локальный сайт для загрузки Word
README.md             эта инструкция
uploads/              загруженные через сайт Word-файлы
outputs/              готовые HTML и JSON-отчеты
```

## Требования

Нужны:

- macOS;
- Python 3;
- LibreOffice.

LibreOffice уже проверен на этой машине по пути:

```text
/Applications/LibreOffice.app/Contents/MacOS/soffice
```

Если LibreOffice нужно установить заново:

```bash
brew install --cask libreoffice
```

После установки LibreOffice нужно перезапустить сайт.

## Быстрый Запуск Сайта

Открыть терминал и выполнить:

```bash
cd "/Users/semen/Курсы обучения/word-to-bitrix"
python3 server.py
```

Открыть в браузере:

```text
http://127.0.0.1:8080
```

Чтобы остановить сайт, в терминале нажать `Ctrl+C`.

Если код инструмента обновлялся, сайт нужно остановить и запустить заново.

## Сценарий Для Аналитика

1. Открыть `http://127.0.0.1:8080`.
2. Нажать выбор файла.
3. Загрузить Word-документ, например `Приложение 6 ККП_июнь.doc`.
4. Нажать `Сконвертировать`.
5. Проверить блок `Предпросмотр`.
6. Проверить, что красные фрагменты исчезли.
7. Проверить, что желтые фрагменты остались без желтой заливки.
8. Проверить таблицы: строки, объединенные ячейки, заголовки, нумерацию.
9. Если все корректно, нажать `Скопировать` в блоке `HTML для Bitrix`.
10. Передать HTML техподдержке или вставить в Bitrix при наличии доступа.

## Сценарий Для Техподдержки

1. Запустить сайт командой из раздела `Быстрый Запуск Сайта`.
2. Загрузить Word-файл через сайт.
3. Проверить статистику результата.
4. Проверить предупреждения, если они есть.
5. Проверить предпросмотр.
6. Скопировать HTML из поля `HTML для Bitrix`.
7. Открыть нужный урок в Bitrix.
8. Вставить HTML в поле содержания урока.
9. Сохранить урок.
10. Открыть пользовательскую страницу и проверить отображение.

## Как Вставлять В Bitrix

В админке Bitrix используется модуль `learning`.

Обычно страница редактирования выглядит так:

```text
/bitrix/admin/learn_unilesson_edit.php
```

Порядок действий:

1. Открыть нужный урок.
2. Перейти на вкладку `Содержание`.
3. Выбрать источник содержимого `текстовое поле`.
4. Выбрать режим `HTML` или режим исходного HTML в визуальном редакторе.
5. Вставить HTML из поля `HTML для Bitrix`.
6. Сохранить урок.
7. Открыть страницу урока для сотрудника и проверить результат.

Основное поле Bitrix:

```text
DETAIL_TEXT
```

## Что Проверять Перед Публикацией

Минимальный чек-лист:

- Нет красных фрагментов, которые должны были быть удалены.
- Желтые фрагменты остались в тексте.
- Желтая подсветка не попала в итоговую публикацию.
- Таблицы не развалились.
- Объединенные ячейки выглядят корректно.
- В таблицах нет пустых строк от удаленных красных данных.
- Нумерация Word сохранилась, например `1.`, `2.`.
- Ссылки открываются.
- В отчете нет критичных предупреждений.
- Пользовательская страница Bitrix выглядит так же, как предпросмотр или ручной эталон.

## Командный Запуск Для Пакетной Проверки

CLI нужен техподдержке для массовой обработки или тестов.

Один файл:

```bash
cd "/Users/semen/Курсы обучения/word-to-bitrix"
python3 bitrix_converter.py "../Word/Приложение 6 ККП_июнь.doc" --out-dir outputs
```

Все `.doc` из папки `Word`:

```bash
cd "/Users/semen/Курсы обучения/word-to-bitrix"
python3 bitrix_converter.py ../Word/*.doc --out-dir outputs
```

Для каждого файла появятся:

```text
outputs/<имя>.html
outputs/<имя>.report.json
```

HTML-файл можно открыть, проверить или вставить в Bitrix.

JSON-отчет содержит:

```text
removed_fragments  что удалено как красное/зачеркнутое
added_fragments    что найдено как желтое/добавленное
warnings           предупреждения
stats              статистика обработки
converter          использованный режим конвертации
```

## Как Понять, Что Все Хорошо

В консоли после CLI-запуска нормальный результат выглядит примерно так:

```text
OK: /Users/semen/Курсы обучения/Word/Приложение 6 ККП_июнь.doc -> outputs/Приложение_6_ККП_июнь.html
{"removed_count": 4, "added_count": 21, "html_length": 178486, "tables_count": 2, "raw_table_unmatched_closing_tags": 0}
```

Важные признаки:

```text
raw_table_unmatched_closing_tags: 0
```

Если значение не `0`, таблицы могли повредиться.

## Проверочные Команды Для Техподдержки

Проверить синтаксис Python:

```bash
cd "/Users/semen/Курсы обучения/word-to-bitrix"
python3 -m py_compile bitrix_converter.py server.py
```

Проверить HTML-таблицы в текущих результатах:

```bash
cd "/Users/semen/Курсы обучения/word-to-bitrix"
python3 - <<'PY'
from pathlib import Path
from bitrix_converter import table_integrity_issues

bad = []
for path in Path('outputs').glob('*.html'):
    html = path.read_text(encoding='utf-8')
    issues = table_integrity_issues(html)
    if issues['unmatched_closing'] or issues['orphan_table_tags']:
        bad.append((path.name, issues))

print('bad', bad)
PY
```

Хороший результат:

```text
bad []
```

Проверить, что в HTML не осталась служебная красная/желтая разметка:

```bash
cd "/Users/semen/Курсы обучения/word-to-bitrix"
python3 - <<'PY'
from pathlib import Path

markers = ['#ff0000', '#ffff00', 'line-through', 'background-color: red', 'background-color: yellow']
bad = []
for path in Path('outputs').glob('*.html'):
    html = path.read_text(encoding='utf-8').lower()
    found = [marker for marker in markers if marker in html]
    if found:
        bad.append((path.name, found))

print('bad', bad)
PY
```

Хороший результат:

```text
bad []
```

## Предупреждения И Что Делать

**Красные/зачеркнутые фрагменты не найдены**

Это не всегда ошибка. Такое бывает, если файл уже очищен вручную или в документе нет удаляемых фрагментов.

**Желтые фрагменты не найдены**

Это не всегда ошибка. Такое бывает, если документ не содержит новых желтых данных.

**Системный конвертер отдал HTML с поврежденной структурой таблиц**

Нельзя публиковать результат без ручной проверки. Нужно убедиться, что LibreOffice установлен и сайт перезапущен.

**В итоговом HTML обнаружены признаки поврежденной таблицы**

Нельзя вставлять результат в Bitrix как готовый. Нужно проверить файл вручную или передать разработчику пример Word-файла.

## Почему Нужен LibreOffice

Старые `.doc` плохо и нестабильно конвертируются системным macOS `textutil`. В сложных таблицах он может закрыть `</table>` раньше времени и оставить `td/tr` вне таблицы.

Текущий надежный путь:

```text
.doc -> LibreOffice -> .docx -> OOXML parser -> clean HTML
```

Так сохраняются:

- таблицы;
- объединенные ячейки;
- цвета правок;
- автонумерация Word.

## Ограничения MVP

- Инструмент не публикует материалы в Bitrix автоматически.
- Инструмент не создает новые разделы и уроки Bitrix.
- Инструмент не проектирует интерактивные блоки `.js-fancyMyText` из произвольного текста Word.
- Инструмент готовит HTML для вставки в существующий урок.
- После конвертации нужен визуальный контроль человеком.

## Что Уже Проверено На Примерах

Проверялись файлы из папки `Word`:

```text
Приложение 6 ККП_июнь.doc
Приложение 6 ККП_июнь_обновленная версия для сравнения.doc
Приложение 6 ККП_март.doc
Приложение 7 ККП_март.doc
Приложение 9 ККТП_март.doc
Приложение 16 ККТП_март .doc
```

На контрольном июньском файле исправлены и проверены:

- удаление красных строк таблицы;
- пересчет `rowspan`;
- сохранение желтых добавлений;
- сохранение номеров `1.` и `2.` из автонумерации Word;
- читаемое форматирование итогового HTML.

## Рекомендации Для Дальнейшего Развития

- Прогнать еще 10-20 реальных файлов.
- Собрать нестандартные случаи в отдельную папку с примерами.
- Добавить автоматическое сравнение с эталонными HTML для ключевых документов.
- Добавить авторизацию, если инструмент будет выкладываться на внутренний сервер.
- Добавить интеграцию с Bitrix API только после стабильной проверки конвертера.
